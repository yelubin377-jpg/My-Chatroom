base64编码？
Redis 是内存数据库,存几个 10MB 文件就吃掉几十 MB 内存。它是缓存,不是文件仓库。

- MySQL 数据在磁盘,内存只是缓存池。能存 LONGBLOB,但让大文件走 SQL 传输又慢又笨。
- 最快的方式恰恰是把文件放磁盘,然后直接用系统调用 sendfile() 让内核把文件→socket 一次拷走,零拷贝。你研究 muduo,这名字你该眼熟——陈硕就是搞这个的。

先快速了解浅层概念

文件传输本质:把A机器磁盘上面的字节流 ， 原封不动版道B机器磁盘上

┌────────────────────────┬────────────────┬────────────────────────────┐
│         子问题         │      白话        │         对应到传输          │
├────────────────────────┼────────────────┼────────────────────────────┤
│ ① 对方怎么知道传的是啥    │ 寄快递先贴运单   │ 先传元数据（文件名、大小）      │
├────────────────────────┼────────────────┼────────────────────────────┤
│ ② 内容怎么过去          │ 装箱、分批搬      │ 分块 + 流式传输              │
├────────────────────────┼────────────────┼────────────────────────────┤
│ ③ 到了怎么知道没坏       │ 签收验货         │ 校验和（MD5/CRC）           │
└────────────────────────┴────────────────┴────────────────────────────┘
核心心智模型：TCP 是一条字节流，没有"这个包到底从哪结束"的天然边界。所以应用层必须自己定义边界——业界通行的做法就是"定长包头 + 长度字段"，这就是所有文件传输协议的雏形（FTP、HTTP 的分块传输、你的 chatroom 打包，都是这个套路）。

这个"已收字节数"，工程上给它起了个名字，叫偏移量（offset）。
断点续传的本质就是：记住偏移量，下次从偏移量接着收。

思路版图：

▎ 文件从 A 到 B（自己走通的版本）：
▎ 1. 读元信息：谁发给谁、文件名、大小
▎ 2. 说明书序列化放前面 + 内容拼成一个包
▎ 3. 说明书只在第一次 send 带，后面只发内容块（同一条连接，TCP 保证顺序）
▎ 4. 内容切块、读一块发一块（send 缓冲区装不下整个文件）
▎ 5. send 塞进内核缓冲区，返回 n = 塞进去多少；光标 c += n
▎ 6. 顺序、丢包、重传 —— TCP 管，不操心
▎ 7. 接收端：说明书拿总大小 → recv 累加已收 → 剩余归零，收完
▎ 8. 全收完再 MD5 验对
▎ 9. 断了 → 记偏移量（已收字节数），重连后告诉服务端从那儿接着发

粘包、分包 https://developer.aliyun.com/article/1509464
         https://www.cnblogs.com/zwj-199306231519/p/13699982.html

学习思考 -- https://blog.csdn.net/qq_32716885/article/details/119807239 's Note
1：文件过大怎么办？

2：传输中断怎么解决？

3：传输过程中，界面卡死怎么处理？

4：文件发送过去，服务端如何处理呢？

Process：
1.定义通讯协议  -- 我已经做到了序列化和反序列化
定义一个json结构发送

```c++
{
"fileName":"测试图片.png",
"fileSize":100
}                 -》可以看到他这里的做法跟我们是比较像的，对了这个一看就不是fwrite


```

2：2.1给服务器发送文件信息  就是我们思路中的那个元信息   -》心智模型
                  

```c++
   FILE* readFile = fopen(spath.c_str(), "rb");  //打开文件
   fseek(readFile, 0, SEEK_END);             //把光标移动道末尾
   int nFileSize = ftell(readFile);			//问问光标现在再哪里 -》文件大小      //
```

   2.2将有效数据组成json结构体字符串数据 --老套路了
              

```c++
   std::string DataSocketParsing::AnalysisFileInforToJson(std::string sfileName, int nfileSize)
                {
	                Json::Value jsData;
	                jsData["fileName"] = sfileName;
	                jsData["fileSize"] = nfileSize;
	                std::string sSendData = jsData.toStyledString();
	                return sSendData;
                }
```

​     ！通讯过程中必须要使用utf8格式的编号。
   2.3给服务器发送信息

```c++
               std::string sJson = m_pDataParsing.AnalysisFileInforToJson("文件名称","文件大小");
​                int nSendJsonSize = sJson.length();
​                int nTotalSize = 27 + nSendJsonSize + 1;
​                char* chSend = new char[nTotalSize];
​                memset(chSend, 0, nTotalSize);
​                this->InitHeaderData(chSend);
​                chSend[21] = 0x00; //服务端编号

                chSend[22] = 0x00;
                chSend[23] = 0xA1; //命令字，占两个字节
     
                chSend[24] = nSendJsonSize /256/256;
                chSend[25] = nSendJsonSize /256;
                chSend[26] = nSendJsonSize %256;
     
                //将json数据字符串拷贝到char*数组中
                memcpy(chSend+27, sJson.c_str(), nSendJsonSize );
                chSend[nTotalSize-1] = 0xEF; 
                chSend[nTotalSize] = '\0';
     
                //socket通讯发送这里不做说明，就是send发送而已
     
                //销毁new出来的内容
                delete[] chSend;
                chSend = nullptr;


```

3.给服务器发送文件内容
    3.1我们需要将文件指针恢复到头位置
      

```c++
  rewind(readFile);  //指针恢复到头位置            //
```

​    3.2定义文件发送时需要的变量

```c++
       int nTotalSize = nFileSize;
        char chFileBuffer[g_nMaxSendByte];
        int nMoveSize = 0; //移动大小
        bool bOK = true;
```

​    3.3读取文件并发送
​        

```c++
      //memset(chFileBuffer, 0, g_nMaxSendByte];
        int nSendSize = nTotalSize <= g_nMaxSendByte ? nTotalSize : g_nMaxSendByte;
       // nMoveSize += nSendSize;
        fread(chFileBuffer, sizeof(char), nSendSize, readFile);
       // memcpy(data + 27, senddata, nsendSize); //发送的json串
        nTotalSize -= nSendSize;
        if (nTotalSize <= 0) 
        {
	       break; //跳出循环
        }
        fseek(readFile, nMoveSize, SEEK_SET); //将指针偏离到头文件 nSendSize个字节处         //
```

3.4完整流程

```c++
while (true)
{
	Sleep(100);

	memset(chFileBuffer, 0, g_nMaxSendByte);
	//获取当前发送的大小
	int nSendSize = nTotalSize <= g_nMaxSendByte ? nTotalSize : g_nMaxSendByte;
	nMoveSize += nSendSize;
	fread(chFileBuffer, sizeof(char), nSendSize, readFile);  
	    int nTotalSize = 27 + nSendSize + 1;
	    char* chSend = new char[nTotalSize];
	    /*
	    这里是组装发送数据与发送文件信息类似，不做详细说明
	    */
	if (m_pTcpPanel->m_ClientThreadInfo.serverSocket != INVALID_SOCKET)
	{
		int nRet = send(serverSocket, chSendData, nsocketSize, 0);
		if (nRet == SOCKET_ERROR)
		{
			//发送失败，停止发送
	                    //每当发送完一次数据时记得要销毁创建的资源	
	            	delete[] chSendData;
	             	chSendData = nullptr;
			bOK = false;
			break;
		}
	}
	else
	{
		bOK = false;
	            //每当发送完一次数据时记得要销毁创建的资源	
	    	delete[] chSendData;
	        chSendData = nullptr;
		break; //socket异常，直接退出
	}
	//每当发送完一次数据时记得要销毁创建的资源	
	delete[] chSendData;
	chSendData = nullptr;
	 
	nTotalSize -= nSendSize;
	if (nTotalSize <= 0)
	{
		break; //跳出循环
	}
	fseek(readFile, nMoveSize, SEEK_SET); //将指针偏离到头文件 nSendSize个字节处

}
fclose(readFile); //结束发送后，需要关闭文件
```

##### 4.服务器接收客户端发送的文件信息

4.1 定义接受数据结构体

```c++
struct ServerDeviceFilesInforSt
{
  int nClientNumber; //客户端编号
  std::string sFileName; //文件名称
  int nTotalFileSize; //文件大小
  int nReceivedSize; //已经接收的大小
  FILE* writeFile; //文件指针
  std::string sSavePath; //记录文件的保存地址
}
```

4.2 记录客户端发送的文件信息

```c++
bool bFind  = false;
std::vector<ServerDeviceFilesInforSt>::iterator itFind = m_vetInfo.begin();
for(itFind; itFind != m_vetInfo.end(); itFind++)
{
  if(itFind->nClientNumber == nDeviceNumber)
  {
     bFind = true; //说明查找到了
     break;
  }
}
```

要注意是不是第一次存储文件！

```c++
if(itFind->nTotalFileSize == 0)
{
  //说明：该编号的客户端文件属于第一次存储
  int nTotalFilesSize = 0;
  std::string sFileName = "";
  /*
  解析客户端发送的内容并获取有效数据，文件名称以及大小
  这里不做说明
  */

   //将解析的内容进行存储
   itFind->sFileName = sFileName;
   itFind->nTotalFileSize = nTotalFilesSize;
   itFind->nReceivedSize = 0; //未进行数据接收

   //创建文件并打开
   std::string sSavePath = "这是当前文件的路径，并是已经创建好的";
   itFind->writeFile = fopen(sSavePath, "wb");
   itFind->sSavePath = sSavePath;
}


```

4.3 记录实际文件信息

当结构体中的文件总体大小不为0时，说明正在接收实际的有效数据，获取接收真实的文件大小

```c++
int nJsonSize = (unsigned char)data[24] * 256 * 256 + (unsigned char)data[25] * 256 + (unsigned char)data[26];
```

将接收的内容存储到文件指针中存储

```c++
fwrite(data + 27, sizeof(char), nJsonSize, itFind->writeFile); 
```

记录到文件指针后记录已经接收的实际长度

```c++
itFind->nReceivedSize += nJsonSize;
```

当服务端记录的文件大小 <= 接收的大小时，说明文件接收完成

```c++
if(itFind->nTotalFileSize <= itFind->nReceivedSize)
{
  fclose(itFind->writeFile); //关闭当前文件
  //文件关闭后清空结构体中的数据
  itFind->nTotalFileSize = 0;
  itFind->nReceivedSize = -1;
  itFind->sSavePath = "";
  itFind->sFileName = "";
}
```



发送文件思路梳理

应用碎片：

```c++
JSON 元信息组装：
// CSDN：拼成字符串返回
std::string sSendData = jsData.toStyledString();
//不用 toStyledString，直接塞 body
Json::Value body;
body["fileName"] = fn;
body["fileSize"] = tot;
client->send(1001, body);   // encode内部序列化
```

```c++
分块里的"组装发送"：
// CSDN：memcpy 进 chSend 字节数组
memcpy(data + 27, senddata, nsendSize);
// base64 塞 JSON body
body["data"] = base64(buf, n);
client->send(1002, body);
```

​	













复盘时终点注意：

1. send() 不是直接把数据发到网上。 它是把数据塞进一个叫"内核发送缓冲区"的东西——一块在系统内存里的队列。真正的发送是内核自己异步干的，send() 只负责"往队列里塞"。    --
2. 所以"发不全"不是网络截断（你担心的那个），而是队列满了塞不进去。这个队列的容量不是固定的"10M"，是系统配置的，通常几十KB到几MB量级。
3. 网络层的 MTU 分片那是另一层的事，应用层你主要撞的是缓冲区。  了解
4. 界面卡死的话怎么处理？
5. 
