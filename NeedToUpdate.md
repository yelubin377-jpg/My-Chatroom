需要维护更新的部分
1.历史记录查询长度，暂时写死了300条 ， 后续用int替换一下LIMIT 300 ，更灵活    牵连诸多文件，并且要读取用户输入更要防止用户奇怪的输入导致系统可能会
exit(1);

2.群组操作 : 一大堆命令居然要记住group_id，过于不现实，后续改进输入groupname到哈希表去找相应的groupid完成操作 !    成功解决，用户值去入输入groupname , 然后后续业务层加了一层判断和转换，把groupname的相应的id从哈希表中读出来再去跑， 复盘时思考：为什么我不直接用群名，更麻烦要加个groupid呢？

3.高并发抗不住 ： 先在仍然是单线程 ， 需要改进成多线程软件 ，加个线程池

4.发现了一个漏洞，就是我的unpack虽然说堆垃圾数据 msg <  MY_PROTO_HEAD_SIZE 这种甚至没有我的协议头长的垃圾信息，虽然会退出，会重新跑，但是没有增加防御机制，会无限循环 
  漏洞处：if(buf->readableBytes() < msgLen || msgLen < MY_PROTO_HEAD_SIZE)
        return nullptr;
        后续需要补漏洞              这里的问题比较大，不是单纯的漏洞，而是没有处理好nullptr判定后要做什么，
                                  前者继续等，后者有极大可能性是恶意行为，一般不会出现，但是一出现就会让整个系统跑不动，这种情况直接shutdown，所以这里判断还不够到位，还要细分，与onMessage那块关联，要大改


5.后续生成多线程后，muduo的Channel tie功能需要更新以保护TcpConnection 
6.在写离线消息的时候突然发现私聊又bug，只有if(Friendconn)的时候才发消息，结果好友离线的时候发了干脆连法都没发，直接只存历史记录  加上离线消息就行ok

7.测试时发现登陆成功后，离线消息推送会发送在登陆成功提示语前面   调换一下顺序就行，以解决

8.测试时发现还有很多bug，再传入要查多少条历史记录的时候，会有一个很大的问题，就是/history吧不管是私聊还是群聊，它不好好输入，输入abc等等而不输入数字，程序会作出不当行为 ： LIMIT 0 直接没反应 ， 或者 LIMIT -1 直接吧整个聊天服务器给杀掉，用户全部掉线!!!
多学到的两个坑：iss >> int 失败置 0、stoi 非数字抛异常、count=-1 能崩服务器。

9.free(): invalid pointer
已中止 (核心已转储)

