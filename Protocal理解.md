首先，要了解一下基本结构  | 2字节协议服务号 |        4字节消息+协议头总长度(6就够用了)    |            消息(Json序列化后的状态)              |
接下来，我们要构建一下基本流程 ， 就是首先，我们需要先声明好基本的框架，
   创建一个MyProtoMsg （要包含基本结构） -> 有多个内容，用class -> 消息最简单 ， 毕竟是Json，直接Json::Value;
   协议头可以再声明一个class MyProtoHead(2字节-》uint16_t协议头服务号 uint32_t总长度) , 然后MyProtoMsg 指向我的MyProtoHead    ///！为什么用u
   (如果要管理协议头比如服务号，MyProtoMsg -> head -> server等等)
   |
   然后，在业务逻辑层，收到指令后(就是调用这个函数的时候) ， 就准备要把信息打包好发回去
   |
   先创建MyProtoMsg， 命名暂时不管；
   |
   然后就可以向MyProtoMsg里面的Json(可以命名位body)天信息，比如状态是成功还是失败？要返回什么message? (eg: MyProtoMsg ... . body[".."] = "..")
   |
   然后可以调用打包函数，因为Json比较乱，需要两个东西，一个是有Json的当接受信息的过渡体，经历上面的流程，第二个是新建的，来存信息      /// ！为什么乱？
   |(打包函数内部逻辑:另外开一块理清思路)
   然后打包好后就可以发回客户端，客户端进行加拆解

打包函数内部逻辑：
    因为又说道需要两个东西来存，先不考虑这么多，首先最简单的一项就是需要把已经存在Json::Value body里的消息先给序列化，用tostyled...和fwriter 二选一
    然后，消息初步处理好后，还有长度和协议服务号要处理
    |
    协议服务号，很简单，从处理函数原始的msg去读   -》注意，这里是 pMsg的协议服务号（也就是map的key）从哪里来，后面还要传到pData去     //
    |
    至于长度，直接就是len = 协议头6 + 消息.size()就行了
    |
    接下来，就是处理这个存消息的新东西，要把过渡体把数据整齐的传到这个新动西里面
    |
    那就要先开辟内存存东西(复盘时思考:为什么pMsg不需要new一块？   -》因为pMsg本质上来说只是一个过渡体，给pData传送数据的中间体，所以暂时不用考虑，在栈上就行了，而pData需要进入网络层，需要活的更就，所以说需要new一块内存，放在堆上面)
    |
    然后，传输协议服务号,最后返回pData就行                       复盘时注意：这里能直接*pData = pMsg->head.server吗？ 不行:

拆包函数内部逻辑:
    拆包就是相当于逆过程，但是首先要注意的点是，小心消息还没发到位直接开始读，容易报错  
    |                                     // 会出现什么错误 ： 如果说运气好就是传送过来的信息是垃圾信息或者信息不全，运气不好就是直接段错误报错
    所以要先检测一下到底有没有 1.把协议体传到位，到位后直接先把协议体给读了
    |
    协议体读完后还需要检测 2.首先消息读完没？如果可读消息大于现在的长度的话不行，整条消息的长度没有协议头长更不行，那就是垃圾信息了，全部重新重新来；
    |
    检测通过后，我们就开始读消息本体了（此时还是序列化状态） 然后，可以用到std::string(指针，长度);从指针位值开始读长度多少的信息存金string里面;
    |
    然后我们可以获得仍然为序列化状态的一串string，然后开始用Json::Reader 反序列化 ， 反序列化后的Json这些东西，传到pMsh->body区域
    |
    然后再把这些协议头传进pMsg
    |
    返回pMsg，让要用的函数自己去取走
    |
    retrieve ，把指针像前移动，不可逆，相当于消费掉了这些东西         //复盘思考：真的不可逆吗？



